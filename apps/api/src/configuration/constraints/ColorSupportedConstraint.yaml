name: ColorSupportedConstraint
rules:
  - keys:
      - true
    conditions:
      relationMode: AND
      conditions:
        - attribute: item.attributes.material
          operator: IN
          value:
            - "{MaterialValues.COLORED_VINYL}"
