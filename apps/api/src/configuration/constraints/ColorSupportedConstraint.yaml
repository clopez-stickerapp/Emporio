name: ColorSupportedConstraint
rules:
  - keys:
      - true
    conditions:
      relationMode: AND
      conditions:
        - attribute: material
          operator: IN
          value:
            - "{MaterialValues.COLORED_VINYL}"
