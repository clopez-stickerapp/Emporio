name: MinSizeConstraint
rules:
  - keys:
      - BIGGER
    conditions:
      - attribute: productName
        operator: IN
        value:
          - "{CustomStickerFamily.PRODUCT_WALL}"
          - "{CustomStickerFamily.PRODUCT_FLOOR}"
