name: material
rules:
  - keys:
      - WHITE_THIN
      - CLEAR_THIN
      - KRAFT_THIN
      - SILVER_THIN
    conditions:
      relationMode: AND
      conditions:
        - attribute: item.productName
          operator: IN
          value:
            - "{ProductNames.PRODUCT_LABELS_ON_ROLL}"
