name: min_size_mm
rules:
  - keys:
      - BIGGER
    conditions:
      relationMode: AND
      conditions:
        - attribute: item.productName
          operator: IN
          value:
            - "{ProductNames.PRODUCT_WALL}"
            - "{ProductNames.PRODUCT_FLOOR}"
