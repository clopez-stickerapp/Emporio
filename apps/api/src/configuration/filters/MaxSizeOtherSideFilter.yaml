name: MaxSizeOtherSideFilter
rules:
  - keys:
      - 275
    conditions:
      - relationMode: and
        conditions:
          - attribute: productName
            operator: in
            value:
              - labels_on_roll

  - keys:
      - 1100
    conditions:
      - relationMode: and
        conditions:
          - attribute: productName
            operator: in
            value:
              - wall