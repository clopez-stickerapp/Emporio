name: MaxSizeOtherSideFilter
mode: highest_score_wins
rules:
  - keys:
      - 275
    conditions:
      relationMode: and
      conditions:
        - attribute: item.productName
          operator: in
          value:
            - labels_on_roll

  - keys:
      - 1100
    conditions:
      relationMode: and
      conditions:
        - attribute: item.productName
          operator: in
          value:
            - wall