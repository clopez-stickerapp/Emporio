{
    "name": "@stickerapp-org/tailwind-config",
    "main": "tailwind.config.js",
    "module": "tailwind.config.js",
    "type": "module",
    "scripts": {
        "lint": "prettier --check .",
        "lint:fix": "npm run format",
        "format": "prettier --write ."
    },
    "devDependencies": {
        "@stickerapp-org/prettier-config": "*"
    }
}
